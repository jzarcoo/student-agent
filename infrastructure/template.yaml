AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: UNAM Student Agent — serverless AI academic assistant

Globals:
  Function:
    Runtime: nodejs22.x
    Architectures: [arm64]
    MemorySize: 256
    Timeout: 120
    PermissionsBoundary: arn:aws:iam::210373500643:policy/hackathon-boundary
    Environment:
      Variables:
        STUDENTS_TABLE: unam-students
        COURSES_TABLE: unam-courses
        PROFESSORS_TABLE: unam-professors
        ROADMAPS_TABLE: unam-roadmaps
        SESSIONS_TABLE: unam-sessions
        RAG_BUCKET: !Ref RagCorpusBucket
        REGION: !Ref AWS::Region

Resources:

  # ── S3 Bucket ──────────────────────────────────────────────────────────────

  RagCorpusBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub unam-rag-corpus-${AWS::AccountId}
      VersioningConfiguration:
        Status: Enabled
      LifecycleConfiguration:
        Rules:
          - Id: expire-reviews-cache
            Status: Enabled
            Prefix: reviews-cache/
            ExpirationInDays: 7
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256

  # ── SQS Queues ─────────────────────────────────────────────────────────────

  ScrapingDLQ:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: unam-scraping-dlq
      MessageRetentionPeriod: 604800

  ScrapingQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: unam-scraping-queue
      VisibilityTimeout: 300
      MessageRetentionPeriod: 86400
      RedrivePolicy:
        deadLetterTargetArn: !GetAtt ScrapingDLQ.Arn
        maxReceiveCount: 3

  NotificationQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: unam-notification-queue
      VisibilityTimeout: 60

  # ── SNS Topic ──────────────────────────────────────────────────────────────

  StudentNotificationsTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: unam-student-notifications
      Subscription:
        - Protocol: sqs
          Endpoint: !GetAtt NotificationQueue.Arn

  # ── Lambda Functions ───────────────────────────────────────────────────────

  OrchestratorFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: unam-agent-orchestrator
      Handler: index.handler
      CodeUri: ../backend/src/orchestrator/
      Policies:
        - DynamoDBCrudPolicy: { TableName: unam-students }
        - DynamoDBCrudPolicy: { TableName: unam-courses }
        - DynamoDBCrudPolicy: { TableName: unam-professors }
        - DynamoDBCrudPolicy: { TableName: unam-roadmaps }
        - DynamoDBCrudPolicy: { TableName: unam-sessions }
        - S3ReadPolicy: { BucketName: !Ref RagCorpusBucket }
        - Statement:
            - Effect: Allow
              Action: [bedrock:InvokeModel, bedrock:InvokeModelWithResponseStream]
              Resource: "*"

  ScraperFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: unam-scraper
      Handler: index.handler
      CodeUri: ../backend/src/scraper/
      Timeout: 300
      MemorySize: 512
      Policies:
        - DynamoDBCrudPolicy: { TableName: unam-courses }
        - DynamoDBCrudPolicy: { TableName: unam-professors }
        - S3CrudPolicy: { BucketName: !Ref RagCorpusBucket }
        - SQSPollerPolicy: { QueueName: !GetAtt ScrapingQueue.QueueName }
      Events:
        SQSTrigger:
          Type: SQS
          Properties:
            Queue: !GetAtt ScrapingQueue.Arn
            BatchSize: 5

  RefreshSchedulerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: unam-schedule-refresh
      Handler: index.handler
      CodeUri: ../backend/src/refresh/
      Policies:
        - SQSSendMessagePolicy: { QueueName: !GetAtt ScrapingQueue.QueueName }
      Events:
        NightlyRefresh:
          Type: Schedule
          Properties:
            Schedule: cron(0 5 * * ? *)
            Description: Nightly refresh of UNAM course schedule data

  # ── API Gateway ────────────────────────────────────────────────────────────

  Api:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: unam-student-agent-api

  ChatResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref Api
      ParentId: !GetAtt Api.RootResourceId
      PathPart: chat

  RootMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref Api
      ResourceId: !GetAtt Api.RootResourceId
      HttpMethod: GET
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        ResponseTransferMode: STREAM
        TimeoutInMillis: 29000
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2021-11-15/functions/${OrchestratorFunction.Arn}/response-streaming-invocations

  ChatMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref Api
      ResourceId: !Ref ChatResource
      HttpMethod: POST
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        ResponseTransferMode: STREAM
        TimeoutInMillis: 29000
        Uri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2021-11-15/functions/${OrchestratorFunction.Arn}/response-streaming-invocations

  Deployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn: [RootMethod, ChatMethod]
    Properties:
      RestApiId: !Ref Api

  Stage:
    Type: AWS::ApiGateway::Stage
    Properties:
      RestApiId: !Ref Api
      DeploymentId: !Ref Deployment
      StageName: prod

  ApiInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt OrchestratorFunction.Arn
      Action: lambda:InvokeWithResponseStream
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${Api}/*

  # ── CloudWatch Alarms ──────────────────────────────────────────────────────

  ScrapingDLQAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: unam-scraping-dlq-not-empty
      MetricName: ApproximateNumberOfMessagesVisible
      Namespace: AWS/SQS
      Dimensions:
        - Name: QueueName
          Value: !GetAtt ScrapingDLQ.QueueName
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold

Outputs:
  ApiUrl:
    Value: !Sub "https://${Api}.execute-api.${AWS::Region}.amazonaws.com/prod/"
  RagBucket:
    Value: !Ref RagCorpusBucket
  ScrapingQueue:
    Value: !Ref ScrapingQueue
  NotificationsTopic:
    Value: !Ref StudentNotificationsTopic
